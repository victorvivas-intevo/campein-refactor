# frontend/dockerfile

# Etapa 1: Build Angular
FROM node:20-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build -- --configuration production

# Etapa 2: Servidor Nginx
FROM nginx:alpine
# Copia el build de Angular a la carpeta de Nginx
COPY --from=build /app/dist/frontend/browser /usr/share/nginx/html
# Copia tu configuración personalizada de Nginx (requerida para SPAs)
COPY nginx.conf /etc/nginx/conf.d/default.conf
# Cloud Run inyecta la variable PORT, pero Nginx escucha en el puerto configurado.
# Usaremos un truco en el CMD para usar el puerto correcto o configurar nginx para el puerto 8080 por defecto.
EXPOSE 8080
CMD ["nginx", "-g", "daemon off;"]